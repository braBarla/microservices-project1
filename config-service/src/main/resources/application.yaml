server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          default-label: eureka
          uri: https://github.com/braBarla/microservices-project1
          search-path: config-data
  application:
    name: config-service
  profiles:
    active: dev,prod